# bound

## Description
The bound libary is a header only library that provides safe arithmetic operations on a number grid. That grid is define by a lower and upper (inclusive) rational bound, hence the name.
The grid has a notch, which is also rational and (upper - lower)/notch is an unsigned integer.

